# TODO

#### Animations
- add slide in/fade out animations that shift left/right for project carousel
- add animations for vertical job carousel (might rework into a timeline style component, but the scroll with that breaks the philosophy of each section being all equal distance)


#### Changes

- redo job carousel
- change click behavior on project cards (only clickable when link exists, as well as manual override on/off link flag per project)
- skeleton loaders
- liquid glass/transparency for components 
- backgrounds (textures, spotlight/vignette, patterns, images - keep current design language of alternating colors and dark-mode only philosophy - avoid using generic addins like spotlights that follow the cursor or interactable backgrounds that visually take away from the actual site)
- scaling of laptop seems small on pc
- jobs carousel is way too high - should be vertically centered
- update to fourth year 
- remove arduino ide callout in about me (keep icon though)
- add placeholder for claude code icon, playwright icon, pnpm, npm, jest, just



#### New features

- login button (only admin exists)
- admin should be able to CRUD project cards with GUI
- admin should be able to CRUD job cards with GUI
- admin should be able to CRUD other text-based components with GUI

- all feature implementation should be FOSS
  
 # less important - do last (vercel does most of this already)
- data logging (traffic)
- data logging (failures)
- data logging dashboard